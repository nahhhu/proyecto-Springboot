meta {
  name: create
  type: http
  seq: 5
}

post {
  url: http://localhost:8080/producto/
  body: json
  auth: inherit
}

body:json {
  {
      "nombre": "Teclado Mecánico Redragon Kumara",
      "precio": 80000,
      "cantidadStock": 10,
      "categoria": "Periférico"
    }
}

settings {
  encodeUrl: true
  timeout: 0
}
